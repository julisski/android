MA	Which of the following are layers a saved item actually travels through on its journey in this app?	The Compose UI (the Save button captures the typed text)	correct	The ViewModel (stamps createdAt, builds the StoredItem, launches a coroutine)	correct	The Repository (the one storage boundary that hands the element to the DAO)	correct	Room / SQLite (the @Insert runs a real INSERT and commits the row to journey.db)	correct	A remote REST web service over the network	incorrect
MA	Why is the DAO's insert(item) declared as a suspend function?	So Room can run the actual disk write off the main thread, keeping the UI smooth	correct	So it can be called from a coroutine (viewModelScope.launch) that pauses until SQLite commits	correct	So it can return the new row's auto-generated id once the write completes	correct	Because you must always wrap a Room call in Dispatchers.IO yourself	incorrect	Because the suspend keyword makes the insert run faster	incorrect
MA	What does KSP (Kotlin Symbol Processing) do in this project?	Generates the concrete ItemDatabase_Impl and ItemDao_Impl classes at build time	correct	Reads Room's @Entity, @Dao, and @Database annotations to produce the SQLite code	correct	Must be applied as a Gradle plugin, or the ksp(room-compiler) dependency does nothing	correct	Runs the app on the device or emulator	incorrect	Encrypts the journey.db file on disk	incorrect
MA	Which statements about the reactive read observeItems(): Flow<List<StoredItem>> are TRUE?	Room re-runs the SELECT and emits a new list every time the items table changes	correct	It means you never have to write a manual 'reload the list' call	correct	It re-emits on deletes as well as inserts	correct	ORDER BY id DESC makes the newest row appear first in the list	correct	Like a suspend write, it returns one value once and then stops	incorrect
MA	How is each StoredItem's primary key id assigned?	The id property defaults to 0 in the data class	correct	@PrimaryKey(autoGenerate = true) tells SQLite to assign the next id on insert	correct	dao.insert(...) returns the new id back to the app as a Long	correct	The ViewModel sets id = System.currentTimeMillis() before saving	incorrect	You must hand-write a CREATE TABLE statement to define the key	incorrect
MA	Which statements about the Room database singleton (ItemDatabase.getInstance) are correct?	It uses double-checked locking inside synchronized so only one instance is ever built	correct	@Volatile makes a write to INSTANCE immediately visible to other threads	correct	It passes applicationContext so it does not leak an Activity	correct	"journey.db" is the name of the SQLite file it creates on disk	correct	Opening two databases over the same file is the recommended approach	incorrect
MA	After you save an item, which actions leave the stored rows INTACT (still present afterward)?	Rotating the screen	correct	Killing the app process and relaunching it	correct	Rebooting the device	correct	Uninstalling the app	incorrect	Tapping 'Clear storage' (DELETE FROM items)	incorrect
MA	Which statements about the items StateFlow in the ViewModel are TRUE?	.stateIn(...) converts Room's cold Flow into a hot StateFlow that always has a value	correct	SharingStarted.WhileSubscribed(5000) keeps it hot for 5s after the last collector leaves	correct	It is given an empty list as its initial value	correct	.onEach { } records the FLOW leg each time Room re-emits a new list	correct	Collecting it blocks the main thread until the database query finishes	incorrect
MA	Why does StorageJourneyViewModel extend AndroidViewModel and use a ViewModelProvider.Factory?	AndroidViewModel supplies an Application Context to build Room without leaking an Activity	correct	The Factory is needed because AndroidViewModel requires the Application be passed in	correct	The ViewModel survives configuration changes such as rotation	correct	viewModelScope is a CoroutineScope tied to the ViewModel's lifetime, used for the suspend writes	correct	The Factory pulls in a dependency-injection library such as Hilt	incorrect
MA	Which statements about the JourneyLog narrator are TRUE?	It holds steps in a MutableStateFlow and exposes a read-only StateFlow via asStateFlow()	correct	It has no Android dependencies, which lets it be unit-tested on the JVM	correct	update { old -> old + step } is an atomic read-modify-write that builds a new list	correct	It uses the same reactive Flow -> Compose pattern Room uses for the data	correct	It persists the journey steps by writing them into the SQLite items table	incorrect
