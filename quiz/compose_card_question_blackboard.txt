MA	Which of the following statements about the Jetpack Compose Material 3 Card composable are TRUE? Select ALL that apply.	Card is a Material 3 container (built on top of Surface) used to group related content, and it applies a themed background color and rounded-corner shape by default.	correct	The content you pass to a Card is a ColumnScope lambda, so by default its children are stacked vertically, the same way a Column lays them out.	correct	A Card can handle taps directly by using the Card overload that takes an onClick parameter (and an enabled flag).	correct	By default a Card lays out its children horizontally, the same way a Row does.	incorrect	A Card has no default shape or container color, so nothing is drawn until you set shape and colors explicitly.	incorrect	A Card cannot respond to clicks on its own; you must wrap it in a Button or attach a separate clickable to make it tappable.	incorrect