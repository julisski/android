MC	In Navigation 3, what is the "back stack"?	A single key that names the one screen the app is allowed to display.	incorrect	An ordered list of keys, where the key on top is the screen currently shown.	correct	A stack of Android Activities, one per screen, managed by the operating system.	incorrect	A cache of bitmap snapshots of every screen the user has visited.	incorrect
MC	You have `val backStack = rememberNavBackStack(HomeKey)`. Which line navigates FORWARD to a new Detail screen?	backStack.removeLastOrNull()	incorrect	backStack.clear()	incorrect	backStack.add(DetailKey(id))	correct	backStack.first()	incorrect
MC	What does `NavDisplay` do in a Navigation 3 app?	It renders whichever key is currently on top of the back stack.	correct	It permanently stores the back stack on disk between app launches.	incorrect	It starts a brand-new Android Activity each time you navigate.	incorrect	It defines the colors and fonts used across the whole app.	incorrect
MC	A "Detail" screen must know WHICH item to show, but the "Home" list screen needs no arguments. How should their navigation keys be declared?	Both as `data object`, since every Nav3 key is a singleton.	incorrect	HomeKey as a `data object`; DetailKey(val itemId: Int) as a `data class`.	correct	Both as `data class`, because a key may never be a `data object`.	incorrect	HomeKey as a `data class`; DetailKey as a `data object`.	incorrect
TF	Pressing the system Back button in a Navigation 3 app typically pops the top key off the back stack (for example, backStack.removeLastOrNull()), returning to the previous screen.	true
MC	Which statement best describes a `@Composable` function and "recomposition"?	It draws the UI exactly once at launch and never runs again.	incorrect	It describes the UI for the current state, and Compose re-runs it (recomposes) when state it reads changes.	correct	It must return an Android View object that you add to an XML layout.	incorrect	It runs on a fixed 60-times-per-second timer regardless of state.	incorrect
MA	Which statements about `remember` and `rememberSaveable` are TRUE? Select ALL that apply.	`remember` keeps a value across recompositions but loses it when the device is rotated (because the Activity is recreated).	correct	`rememberSaveable` preserves its value across rotation and process death.	correct	A simple Boolean or Int can be stored in `rememberSaveable` with no extra code.	correct	`remember` automatically saves its value to disk and restores it after the app is killed.	incorrect	`rememberSaveable` is only for navigation keys and cannot hold ordinary UI state.	incorrect
MC	A screen composable takes its data as parameters and reports user actions through callbacks (such as onClick), holding no state of its own. What is this pattern called?	State hoisting / unidirectional data flow: state flows down, events flow up.	correct	Two-way data binding, where the UI edits the underlying data directly.	incorrect	Imperative UI, where you mutate view objects by hand.	incorrect	Inheritance, where the screen extends a base State class.	incorrect
MC	Does the ORDER of modifiers matter? Compare `Modifier.padding(16.dp).background(Blue)` with `Modifier.background(Blue).padding(16.dp)`.	No — Compose sorts modifiers automatically, so order never matters.	incorrect	Yes — order matters; the two chains differ (for example, whether the padding sits inside or outside the blue area).	correct	No — padding and background can never appear in the same chain.	incorrect	Yes, but only for clickable; every other modifier ignores order.	incorrect
MC	How does a `LazyColumn` differ from a regular `Column` when showing a long list?	It composes every item in memory up front, but only draws (clips) the ones currently on screen.	incorrect	It re-composes the entire list from scratch on every scroll frame.	incorrect	It only composes and lays out the items currently visible, so long lists stay fast.	correct	It composes all items eagerly, exactly like a Column.	incorrect
