MC	In these Navigation 3 projects (NavDetailList, NavThreeScreen, NavFourScreen), which composable renders whichever key is currently on top of the back stack?	NavDisplay	correct	NavHost	incorrect	Scaffold	incorrect	NavController	incorrect	LazyColumn	incorrect
MC	In AppNavigation, which call creates the back stack, seeds it with the first screen's key, and preserves it across recomposition and rotation?	remember { mutableStateOf(CategoriesKey) }	incorrect	rememberNavBackStack(CategoriesKey)	correct	rememberNavController()	incorrect	rememberSaveable { CategoriesKey }	incorrect	NavBackStack(CategoriesKey)	incorrect
MC	CategoriesKey is declared as a `data object` but DetailKey is a `data class`. Why the difference?	A data object is not allowed to implement the NavKey interface at all	incorrect	A data class loads measurably faster than a data object would in Nav3	incorrect	DetailKey needs an itemId argument; the list screen needs none	correct	The very first screen in any Nav3 app must always be a plain data object	incorrect	Only a data class, and never a data object, can be marked @Serializable	incorrect
MC	In NavDetailList, tapping a row runs onOpen = { id -> backStack.add(DetailKey(id)) }. What does this do?	It calls navController.navigate("detail") and passes the id along	incorrect	It adds a key to the back stack for NavDisplay to match	correct	It fires an Intent that starts a separate Activity with the id	incorrect	It directly invokes the DetailScreen composable function by name	incorrect	It tears down the current NavDisplay and builds a replacement	incorrect
MC	On the NavFourScreen Fact screen, "Start over" runs `while (backStack.size > 1) backStack.removeLastOrNull()`. What is the result?	The app exits because the back stack is emptied out completely	incorrect	Only the single top key pops, so it goes back one screen	incorrect	The whole back stack is duplicated, doubling every screen	incorrect	All keys but the first pop, returning to the root	correct	It navigates forward and opens a brand-new fifth screen	incorrect
MC	Inside entryProvider, what does the block `entry<DetailKey> { key -> DetailScreen(...) }` do?	It registers the screen for that key and passes the key in	correct	It renders DetailScreen immediately when the app first launches	incorrect	It permanently removes every DetailKey from the navigation stack	incorrect	It converts the DetailKey data class into a data object instead	incorrect	It sets the system back-button behavior shared by every screen	incorrect
MC	Why do the navigation keys carry only an id (e.g. DetailKey(itemId)) instead of the whole Item object?	Navigation 3 keys are strictly forbidden from holding properties	incorrect	Passing the whole object is faster but drains far more battery	incorrect	The id stays small and serializable; the screen looks it up	correct	The Item class simply cannot be referenced inside any composable	incorrect	The id happens to be the only field the Item data class contains	incorrect
MC	In NavViewModelState, why does the favorites set in FavoritesViewModel survive a screen rotation while a plain `remember {}` value would not?	The remember block automatically clears its value every few seconds	incorrect	It is scoped to the Activity and re-attached after recreation	correct	The ViewModel writes each value out to a remote server on change	incorrect	Rotation never actually recreates the Activity, so nothing is lost	incorrect	A ViewModel keeps its data inside the serialized navigation key	incorrect
MC	In NavViewModelState's unidirectional data flow, how does the Detail screen change a favorite?	The Detail composable mutates the favorites Set directly in place	incorrect	It writes the change into a rememberSaveable variable on screen	incorrect	It edits the static sampleItems list so the change goes global	incorrect	It calls viewModel.toggleFavorite(id); state flows back down	correct	It pushes a new key onto the back stack to record the favorite	incorrect
MC	In NavDataLayer, the list screen renders from a sealed PlanetsUiState. Which set of states does it model?	Loading, Empty, Error, and Success	correct	Start, Running, Paused, and Stopped	incorrect	Foreground, Background, Created, and Destroyed	incorrect	Push, Pop, Peek, and Clear	incorrect	Idle, Tap, Drag, and Release	incorrect
